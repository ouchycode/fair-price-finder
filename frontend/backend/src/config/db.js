// Konfigurasi database (isi sesuai pilihan DB tim)
// Contoh: MongoDB, PostgreSQL, atau SQLite

module.exports = {
  // mongodb: process.env.MONGO_URI
  // postgres: { host, port, user, password, database }
};
